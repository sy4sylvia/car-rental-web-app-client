import React, {useState} from 'react';

function CheckoutContainer(){

    // In the <CheckoutContainer /> component
    // you will be generating the checkout token.
    // You will use the commerce.checkout.generateToken() helper function
    // and wrap this in an useEffect() - so that any time this component is loaded,
    // you will always set the live object in state:


    return (
        <div className="container">

        </div>
    );
}

export default CheckoutContainer;